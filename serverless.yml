service: serverless-quotes-api

provider:
  name: aws
  runtime: nodejs18.x
  region: eu-west-1
  environment:
    QUOTES_TABLE: ${self:service}-quotes

plugins:
  - serverless-offline

package:
  exclude:
    - node_modules/**
    - src/**
    - test/**

functions:
  createQuoteWithSections:
    handler: dist/src/handlers/QuoteHandlers/createQuoteWithSections.handler
    timeout: 30
    events:
      - http:
          path: quotes
          method: post
  updateQuote:
    handler: dist/src/handlers/QuoteHandlers/updateQuote.handler
    timeout: 30
    events:
      - http:
          path: quotes/{quoteId}
          method: put
          


resources:
  Resources:
    QuotesTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:provider.environment.QUOTES_TABLE}
        AttributeDefinitions:
          - AttributeName: quoteId
            AttributeType: S
        KeySchema:
          - AttributeName: quoteId
            KeyType: HASH
        ProvisionedThroughput:
          ReadCapacityUnits: 1
          WriteCapacityUnits: 1
